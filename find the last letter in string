#include <stdio.h>
#include<string.h>
int main(void) {
    // your code goes here
	char s[100];
	scanf("%s",s);
	int x=strlen(s);
	
	if(s[x-1]=='r'){
		printf("er");
	}
	else 
		printf("ist");
	
	
    return 0;
}
