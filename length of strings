#include<stdio.h>
#include<string.h>
int main()
{
int n;
scanf("%d",&n);
char str[n][10000];
int i,j;
for (i=0;i<n;i++)
    scanf("%s",str[i]);
for (i=0;i<n;i++){
    int len=0;
    for (j=0;str[i][j]!='\0';j++){
        len++;
    }
    printf("%d\n",len);
}
return 0;
}    
